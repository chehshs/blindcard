# コードレビュー — blindcard / ANKI NOTE MAKER

- 対象: `app.py` / `pdf_generator.py` / `make_pdf.py` / `templates/index.html` / `templates/app.js` / `run*.command`
- 初回レビュー: 2026-09-08
- 修正確認: 2026-09-08（実際にコードを動かして再検証）
- 検証環境: Python 3.9 系, Flask 3.1.3, reportlab 5.0.1, pandas 2.3.3

## 対応状況サマリ

| # | 指摘 | 区分 | 状態 |
|---|---|---|---|
| 1-1 | 連番CSVで1行＝1ページになる | バグ | ✅ 修正確認 |
| 1-2 | 番号の `&` が `&amp;` と印字される | バグ | ✅ 修正確認 |
| 1-3 | CSVヘッダー誤判定でデータが消える | バグ | ✅ 修正確認 |
| 1-4 | Shift_JIS のCSVが扱えない | バグ | ✅ 修正確認 |
| 1-5 | 長文が行からはみ出して重なる | バグ | ✅ 修正確認 |
| 1-6 | 2000行超の無言切り捨て／見積りのズレ | バグ | ✅ 修正確認 |
| 2-1 | `debug=True` | セキュリティ | ✅ 修正確認 |
| 2-2 | CORS ワイルドカード | セキュリティ | ✅ 修正確認 |
| 2-3 | 例外の握り潰し | 保守性 | ✅ 修正確認 |
| 3-1 | `run.command` が `.venv` を見ない | 運用 | ✅ 修正確認 |
| 3-2 | Tailwind / Google Fonts がCDN依存 | 運用 | ⬜ 未対応 |
| 3-3 | 日本語フォント非埋め込み | 運用 | ⬜ 未対応 |
| 3-4 | 重複ファイル | 整理 | ✅ 削除確認 |
| 3-5 | `git init` されていない | 整理 | 🔶 init済み・未コミット |
| 3-6 | テストなし | 品質 | ✅ 追加確認 |

---

## 1. バグ（すべて修正確認済み）

### ✅ 1-1. 番号が「1, 2, 3…」形式のCSVだと 1行＝1ページになる

**現象**: `pdf_generator.py` のセクション分割が `-` の前でグループ化していたため、連番だと全行が別セクション扱いになっていた。

**修正**: `_section_key()` がハイフンを含む番号だけをセクションキーとして扱うようになり、連番・番号なしは同一グループに。`estimate_pages()` を新設して `_group_rows()` をフロント/バックで共通の考え方に揃えた点も良い。

**検証（見積り vs 実PDFのページ数）**:

| 番号形式 | 見積 | 実PDF | 判定 |
|---|---|---|---|
| 連番 1..10 | 2 | 2 | ✅（旧: 10ページ）|
| `1-1` 形式 x10 | 2 | 2 | ✅ |
| `1-x5` + `2-x5` | 2 | 2 | ✅ |
| 交互 `1-1,2-1,1-2,2-2` | 4 | 4 | ✅ |
| 番号なし x10 | 2 | 2 | ✅ |
| 混在 `1,2,1-1,1-2` | 2 | 2 | ✅ |

### ✅ 1-2. `&` `<` `>` を含む番号がPDFに `&amp;` と印字される

**修正**: `_clip_text()` と `_safe_text()` を分離し、Paragraph に渡す `question` / `answer` / `note` のみエスケープ、`drawString` に渡る `num` は生テキストのまま保持。

**検証**: 実PDFを目視。`小1&2` / `A&B` / `<注意>` / タイトル `漢字 & 語句` すべて正しく印字。

### ✅ 1-3. CSV取り込みで1行目が誤ってヘッダー判定される

**修正**: 判定語を `番号 / 問題 / 解答 / 備考 / num / question / answer / note` の完全一致に絞り、さらに「1列目に数字があればデータ行」の条件を追加。

**検証（7ケース、すべて期待どおり）**:

| 入力 | 判定 |
|---|---|
| `["番号","書き（カタカナ入り例文）","漢字（対象漢字）"]` | true（ヘッダー）✅ |
| `["1-1","漢字の書き取り","練習"]` | false ✅（旧バグ: true で1行消えていた）|
| `["1-1","今日はキカイだ","機会"]` | false ✅ |
| `["","問題","解答"]` | true ✅ |
| `["1-1","問題","解答"]` | false ✅ |
| `["num","question","answer","note"]` | true ✅ |
| `["番号ID","例文","答え"]` | false（既知の限界。下記メモ参照）|

### ✅ 1-4. Shift_JIS のCSVが扱えない

**修正**:
- CLI: `read_anki_csv()` が `utf-8-sig` → `cp932` の順にフォールバック
- Web: `readAsArrayBuffer` + `decodeCsvBuffer()` で `utf-8(fatal)` → `shift_jis` にフォールバック

**検証**: CP932 バイト列 `94 D4 8D 86` が両方で「番号」に復元されることを確認。CLI のCP932読み込みはユニットテストでもカバー済み。

### ✅ 1-5. 長い問題文が行からはみ出して隣の行に重なる

**修正**: `_fit_paragraph()` を新設。行高を超える場合はフォントを縮小し、それでも収まらなければ二分探索で省略記号を付けて切り詰める。

**検証（A6・10行組、行高 34.1pt）**:

| 文字数 | 修正前 | 修正後 |
|---|---|---|
| 40文字 | 26.9pt OK | 26.9pt ✅ |
| 80文字 | 44.9pt ★はみ出し | 26.2pt ✅ |
| 150文字 | 89.8pt ★はみ出し | 33.0pt ✅ |
| 500文字 | ★はみ出し | 33.0pt ✅ |
| 2000文字 | ★はみ出し | 33.0pt ✅ |

**性能への影響**: 2000件 × 200文字で 1.82秒 / 200ページ。実データ349件なら 0.06秒。実用上問題なし。

### ✅ 1-6. 2000行超の無言切り捨て／ページ数見積りのズレ

**修正**: `_normalize_rows()` が上限超過時に件数付きの `ValueError` を送出（`estimate_pages` は `enforce_limit=False` で除外）。フロント側にも 2000件ガードを追加。見積りロジックは 1-1 の対応で実装と一致。

**検証**: 2001件で `ValueError: データが2001件あります。一度に出力できるのは2000件までです。` を確認。

---

## 2. 設定・セキュリティ（すべて修正確認済み）

- ✅ **2-1 `debug=True`** → `ANKI_DEBUG` 環境変数で明示的に有効化する方式に変更。既定はオフ。
- ✅ **2-2 CORS ワイルドカード** → `after_request` のCORSヘッダーを撤去。`OPTIONS` ハンドラと `app.js` の `apiBase` フォールバックも合わせて削除され、同一オリジン前提に統一。
- ✅ **2-3 例外の握り潰し** → `app.logger.exception("PDF 生成に失敗しました")` を追加。

---

## 3. 構成・運用

- ✅ **3-1 `run.command` が `.venv` を見ない** → `run_web.command` と同じvenv優先ロジックに統一。
- ⬜ **3-2 Tailwind / Google Fonts がCDN依存** — 未対応。`static/` は空のままで、さらに **Font Awesome がCDN依存として追加** されている（`templates/index.html:12`）。オフラインではレイアウトとアイコンが崩れる。ビルド済みCSSとアイコンを `static/` に置くと、Vueをローカル同梱している方針と一貫する。
- ⬜ **3-3 日本語フォント非埋め込み** — 未対応。`HeiseiMin-W3`（CIDフォント参照）のままなので、閲覧側・印刷側の環境依存は残る。配布やコンビニ印刷を想定するなら IPAex明朝 / Noto Serif JP を同梱して `TTFont` で埋め込む。
- ✅ **3-4 重複ファイル** → ルート直下の `kanji_matched.csv` と `kanji_notebook_mincho_orange_a6.pdf` の削除を確認。
- 🔶 **3-5 `git init`** → リポジトリ化は完了。ただし**まだ1コミットもない**（`main` ブランチにコミットなし）ので、現状を初回コミットとして固定しておきたい。
- ✅ **3-6 テスト** → `tests/test_anki.py`（13件）と `tests/test_csv_header.js`（6ケース）を追加。実行してすべてパスを確認。ページ分割・エスケープ・用紙スケール・CP932・APIバリデーションをカバーしている。

---

## 4. E2E 動作確認

サーバーを起動しブラウザから操作:

- `GET /` 200、コンソールエラーなし
- 「PDFを発行する」→ `POST /api/generate-pdf` 200、成功トースト表示、ダウンロード動作
- UI が刷新され、レイアウト設定・データリスト・プレビューの導線も問題なし

---

## 5. 追加で気づいた細かい点（任意）

- **縮小テキストが罫線と接近する**: `_fit_paragraph` が行高いっぱい（34.1pt中33.0pt）まで使うため、長文の行は上下の点線とほぼ接する。`max_height` を `row_h * 0.9` 程度にすると余白が出て読みやすい。
- **巨大ペイロードの 413**: 2.7MB のリクエストは Flask が HTML で 413 を返すため、フロントのメッセージが汎用文言になる。実データ2000件でも 0.49MB なので実害はほぼないが、`errorhandler(413)` でJSONを返すと親切。
- **ヘッダー判定の限界**: `["番号ID","例文","答え"]` のような独自ヘッダーは検出できず1行目がデータとして取り込まれる。取り込み後に表で修正できるため実用上の影響は小さい。
- **「記入 0 件 / 予想 1 ページ」**: 既定の5行が番号のみ埋まっているため。番号だけの行も罫線として印刷される仕様なら想定どおり。

---

## 6. 良かった点

- `validate_options` に検証を集約し、CLI・Web の両方が同じ経路を通る構造
- 今回の修正で `estimate_pages` / `_group_rows` を共有し、フロントとバックのページ計算の二重実装を解消した点
- 用紙サイズを幅比で `scale` 化して A6 / A5 / A4 を1コードで賄っている点（A系は相似なので破綻しない）
- `v-cloak` + `__ANKI_MOUNTED` によるブート失敗時のフォールバック表示
- 生成物の見た目が良好。オレンジ解答＋点線区切り＋中央の縦罫は赤シート運用に素直
