# コードレビュー — blindcard / BlindCardMaker

- 対象: `app.py` / `pdf_generator.py` / `make_pdf.py` / `templates/index.html` / `templates/guide.html` / `templates/app.js` / `run*.command`
- 初回レビュー: 2026-09-08
- 修正確認: 2026-09-08（1次: バグ6件＋セキュリティ／2次: オフライン対応・フォント埋め込み。いずれも実際にコードを動かして再検証）
- 3次レビュー: 2026-09-08（UI刷新後の `http://127.0.0.1:5000/` を実機操作。→ 第8章）
- 検証環境: Python 3.9 系, Flask 3.1.3, reportlab 5.0.1, pandas 2.3.3

## 対応状況サマリ

| # | 指摘 | 区分 | 状態 |
|---|---|---|---|
| 1-1 | 連番CSVで1行＝1ページになる | バグ | ✅ 修正確認 |
| 1-2 | 番号の `&` が `&amp;` と印字される | バグ | ✅ 修正確認 |
| 1-3 | CSVヘッダー誤判定でデータが消える | バグ | ✅ 修正確認 |
| 1-4 | Shift_JIS のCSVが扱えない | バグ | ✅ 修正確認 |
| 1-5 | 長文が行からはみ出して重なる | バグ | ✅ 修正確認 |
| 1-6 | 2000行超の無言切り捨て／見積りのズレ | バグ | ✅ 修正確認 |
| 2-1 | `debug=True` | セキュリティ | ✅ 修正確認 |
| 2-2 | CORS ワイルドカード | セキュリティ | ✅ 修正確認 |
| 2-3 | 例外の握り潰し | 保守性 | ✅ 修正確認 |
| 3-1 | `run.command` が `.venv` を見ない | 運用 | ✅ 修正確認 |
| 3-2 | Tailwind / Google Fonts がCDN依存 | 運用 | ✅ 修正確認 |
| 3-3 | 日本語フォント非埋め込み | 運用 | ✅ 修正確認 |
| 3-4 | 重複ファイル | 整理 | ✅ 削除確認 |
| 3-5 | `git init` されていない | 整理 | 🔶 初回コミット済み・作業中の変更が未コミット |
| 3-6 | テストなし | 品質 | ✅ 追加確認（15件）|
| 5-1 | 縮小テキストが罫線と接近 | 仕上げ | ✅ 修正確認 |
| 5-2 | 巨大ペイロードの 413 がHTML | 仕上げ | ✅ 修正確認 |
| 5-3 | README がない | 文書 | ⬜ 未対応 |

---

## 1. バグ（すべて修正確認済み）

### ✅ 1-1. 番号が「1, 2, 3…」形式のCSVだと 1行＝1ページになる

**現象**: `pdf_generator.py` のセクション分割が `-` の前でグループ化していたため、連番だと全行が別セクション扱いになっていた。

**修正**: `_section_key()` がハイフンを含む番号だけをセクションキーとして扱うようになり、連番・番号なしは同一グループに。`estimate_pages()` を新設して `_group_rows()` をフロント/バックで共通の考え方に揃えた点も良い。

**検証（見積り vs 実PDFのページ数）**:

| 番号形式 | 見積 | 実PDF | 判定 |
|---|---|---|---|
| 連番 1..10 | 2 | 2 | ✅（旧: 10ページ）|
| `1-1` 形式 x10 | 2 | 2 | ✅ |
| `1-x5` + `2-x5` | 2 | 2 | ✅ |
| 交互 `1-1,2-1,1-2,2-2` | 4 | 4 | ✅ |
| 番号なし x10 | 2 | 2 | ✅ |
| 混在 `1,2,1-1,1-2` | 2 | 2 | ✅ |

### ✅ 1-2. `&` `<` `>` を含む番号がPDFに `&amp;` と印字される

**修正**: `_clip_text()` と `_safe_text()` を分離し、Paragraph に渡す `question` / `answer` / `note` のみエスケープ、`drawString` に渡る `num` は生テキストのまま保持。

**検証**: 実PDFを目視。`小1&2` / `A&B` / `<注意>` / タイトル `漢字 & 語句` すべて正しく印字。

### ✅ 1-3. CSV取り込みで1行目が誤ってヘッダー判定される

**修正**: 判定語を `番号 / 問題 / 解答 / 備考 / num / question / answer / note` の完全一致に絞り、さらに「1列目に数字があればデータ行」の条件を追加。

**検証（7ケース、すべて期待どおり）**:

| 入力 | 判定 |
|---|---|
| `["番号","書き（カタカナ入り例文）","漢字（対象漢字）"]` | true（ヘッダー）✅ |
| `["1-1","漢字の書き取り","練習"]` | false ✅（旧バグ: true で1行消えていた）|
| `["1-1","今日はキカイだ","機会"]` | false ✅ |
| `["","問題","解答"]` | true ✅ |
| `["1-1","問題","解答"]` | false ✅ |
| `["num","question","answer","note"]` | true ✅ |
| `["番号ID","例文","答え"]` | false（既知の限界。下記メモ参照）|

### ✅ 1-4. Shift_JIS のCSVが扱えない

**修正**:
- CLI: `read_anki_csv()` が `utf-8-sig` → `cp932` の順にフォールバック
- Web: `readAsArrayBuffer` + `decodeCsvBuffer()` で `utf-8(fatal)` → `shift_jis` にフォールバック

**検証**: CP932 バイト列 `94 D4 8D 86` が両方で「番号」に復元されることを確認。CLI のCP932読み込みはユニットテストでもカバー済み。

### ✅ 1-5. 長い問題文が行からはみ出して隣の行に重なる

**修正**: `_fit_paragraph()` を新設。行高を超える場合はフォントを縮小し、それでも収まらなければ二分探索で省略記号を付けて切り詰める。

**検証（A6・10行組、行高 34.1pt）**:

| 文字数 | 修正前 | 修正後 |
|---|---|---|
| 40文字 | 26.9pt OK | 26.9pt ✅ |
| 80文字 | 44.9pt ★はみ出し | 26.2pt ✅ |
| 150文字 | 89.8pt ★はみ出し | 33.0pt ✅ |
| 500文字 | ★はみ出し | 33.0pt ✅ |
| 2000文字 | ★はみ出し | 33.0pt ✅ |

**性能への影響**: 2000件 × 200文字で 1.82秒 / 200ページ。実データ349件なら 0.06秒。実用上問題なし。

### ✅ 1-6. 2000行超の無言切り捨て／ページ数見積りのズレ

**修正**: `_normalize_rows()` が上限超過時に件数付きの `ValueError` を送出（`estimate_pages` は `enforce_limit=False` で除外）。フロント側にも 2000件ガードを追加。見積りロジックは 1-1 の対応で実装と一致。

**検証**: 2001件で `ValueError: データが2001件あります。一度に出力できるのは2000件までです。` を確認。

---

## 2. 設定・セキュリティ（すべて修正確認済み）

- ✅ **2-1 `debug=True`** → `ANKI_DEBUG` 環境変数で明示的に有効化する方式に変更。既定はオフ。
- ✅ **2-2 CORS ワイルドカード** → `after_request` のCORSヘッダーを撤去。`OPTIONS` ハンドラと `app.js` の `apiBase` フォールバックも合わせて削除され、同一オリジン前提に統一。
- ✅ **2-3 例外の握り潰し** → `app.logger.exception("PDF 生成に失敗しました")` を追加。

---

## 3. 構成・運用

- ✅ **3-1 `run.command` が `.venv` を見ない** → `run_web.command` と同じvenv優先ロジックに統一。
- ✅ **3-2 Tailwind / Google Fonts がCDN依存** → Tailwind CLI（`package.json` の `build:css`）でビルドした `static/app.css` をローカル配信する方式に変更。Google Fonts と Font Awesome の参照も撤去。
  - **検証**: テンプレートとCSSに外部ホスト参照が1件も残っていないことを確認。ブラウザで実際に開き、通信が `127.0.0.1` の4リクエスト（`/`, `/static/app.css`, `/vue.global.prod.js`, `/app.js`）のみであること、コンソールエラーなし、レイアウトが崩れていないことを確認。
- ✅ **3-3 日本語フォント非埋め込み** → `fonts/ipaexm.ttf`（IPAex明朝）を同梱し、`_ensure_font()` が TTF があれば `TTFont` で登録、無ければ従来の CID フォントにフォールバックする二段構え。ライセンス全文も同梱済み。
  - **検証**: 生成PDFに `/FontFile2` が含まれ、`/BaseFont /AAAAAA+IPAexMincho` とサブセット埋め込みされていることを確認。サイズは1件で22KB、実CSV 348件で447KB（0.11秒）と実用範囲。
- ✅ **3-4 重複ファイル** → ルート直下の `kanji_matched.csv` と `kanji_notebook_mincho_orange_a6.pdf` の削除を確認。
- 🔶 **3-5 `git init`** → 初回コミット `b166f87`「初期実装を固定し、オフライン表示と日本語フォント埋め込みに対応する。」を確認。ただし `app.py` / `templates/index.html` / `static/app.css` / `static/input.css` の4ファイルが**未コミットのまま**なので、区切りとしてコミットしておきたい。
- ✅ **3-6 テスト** → `tests/test_anki.py`（15件）と `tests/test_csv_header.js`（6ケース）を追加。実行してすべてパスを確認。ページ分割・エスケープ・用紙スケール・CP932・APIバリデーション・同梱フォントの存在をカバーしている。
- ⬜ **5-3 README がない** — 未対応。`npm run build:css` が必要なこと、`static/app.css` がビルド成果物であること（HTMLのクラスを変えたら再ビルドが要る）、`fonts/` のライセンス、`run_web.command` での起動手順がどこにも書かれていない。

---

## 4. E2E 動作確認

サーバーを起動しブラウザから操作:

- `GET /` 200、コンソールエラーなし
- 「PDFを発行する」→ `POST /api/generate-pdf` 200、成功トースト表示、ダウンロード動作
- UI が刷新され、レイアウト設定・データリスト・プレビューの導線も問題なし
- CDN撤去後に再確認: 外部通信ゼロ（`127.0.0.1` の4リクエストのみ）、表示崩れなし

---

## 5. 仕上げの項目

- ✅ **5-1 縮小テキストが罫線と接近する** → `ROW_TEXT_HEIGHT_RATIO = 0.9` を導入し、行高の9割を上限に収めるよう変更。実PDFで上下に余白が確保されたことを目視確認。
- ✅ **5-2 巨大ペイロードの 413** → `errorhandler(413)` を追加。2.7MB のリクエストで `413 / application/json / {"error": "送信データが大きすぎます。件数を減らすか、CSVを分割してください。"}` が返ることを確認。
- ⬜ **5-3 README がない** → 上記 3. を参照。
- **ヘッダー判定の限界**: `["番号ID","例文","答え"]` のような独自ヘッダーは検出できず1行目がデータとして取り込まれる。取り込み後に表で修正できるため実用上の影響は小さい。
- **「記入 0 件 / 予想 1 ページ」**: 既定の5行が番号のみ埋まっているため。番号だけの行も罫線として印刷される仕様なら想定どおり。

---

## 6. 良かった点

- `validate_options` に検証を集約し、CLI・Web の両方が同じ経路を通る構造
- 今回の修正で `estimate_pages` / `_group_rows` を共有し、フロントとバックのページ計算の二重実装を解消した点
- 用紙サイズを幅比で `scale` 化して A6 / A5 / A4 を1コードで賄っている点（A系は相似なので破綻しない）
- `v-cloak` + `__ANKI_MOUNTED` によるブート失敗時のフォールバック表示
- 生成物の見た目が良好。オレンジ解答＋点線区切り＋中央の縦罫は赤シート運用に素直


---

## 7. 現時点の残件

1. ⬜ **README の作成**（5-3）— ビルド手順・起動手順・フォントライセンスの明記
2. 🔶 **未コミットの4ファイルをコミット**（3-5）
3. （任意）`FONT_NAME` を `_ensure_font()` で書き換えるモジュール変数にしている点は、`from pdf_generator import FONT_NAME` のような取り込み方をすると登録前の値を掴むため、将来的には関数の戻り値で受け渡す形の方が安全

---

# 8. 3次レビュー（UI刷新 / BlindCard 版）

`http://127.0.0.1:5000/` と `/guide` を実際に操作して確認した内容。ブランド名が **BlindCardMaker** に変わり、`/guide`（使い方ページ）とロゴ、配色（ミント系）、空状態のUIが追加されている。

## 8-1. 良かった点

- **空状態の設計** — カードが0件のとき「プレビュー」「PDFを発行する」がいずれも `disabled`。ガイドの記述とも整合している。
- **モバイル対応** — 375px 幅で計測して `scrollWidth - clientWidth = 0`、ビューポートをはみ出す要素は0件。ハンバーガーから Print Settings を開く導線もある。
- **外部依存ゼロを維持** — 新規の `templates/guide.html` にも外部URLの参照なし。
- **アクセシビリティの前進** — 色スウォッチに `aria-label`（ローズ/オレンジ/赤/マゼンタ）。モーダルは Escape キーで閉じる。
- **`/guide` が実質マニュアルとして機能** — 番号のまとまりと改ページ規則、対応文字コード、2000件上限、独自見出しCSVの挙動まで実装どおりに記述されている。
- **E2E** — CSVドロップ → 3件取り込み → PDF発行が 74ms で成功。

## 8-2. 要対応

| # | 指摘 | 区分 | 状態 |
|---|---|---|---|
| 8-A | ユニットテストが2件失敗している | バグ | ⬜ 未対応 |
| 8-B | プレビューが実PDFの1ページ目と一致しない | バグ | ⬜ 未対応 |
| 8-C | ガイドの記述が実装と食い違う（2か所）| 文書 | ⬜ 未対応 |
| 8-D | 既定色の変更で赤シートの隠蔽性が低下 | 設計判断 | ⬜ 要判断 |
| 8-E | favicon の指定がない | 仕上げ | ⬜ 未対応 |
| 8-F | モーダルに `role="dialog"` がない | a11y | ⬜ 未対応 |

### ⬜ 8-A. ユニットテストが2件失敗している ★最優先

```bash
python3 -m unittest discover -s tests -p "test_*.py"
# FAIL: test_index_links_to_guide (test_anki.PageTests)
# FAIL: test_guide_page (test_anki.PageTests)
# Ran 17 tests / FAILED (failures=2)
```

どちらも `/static/blindcard-maker.svg` がページ内で参照されていることを期待しているが、`index.html` と `guide.html` は文字「B」のマークを使っており、**この SVG はどこからも参照されていない**（ファイル自体は存在し、配信も 200）。テストを現状に合わせるか、ロゴを実際に使うかの判断が必要。

### ⬜ 8-B. プレビューが実PDFの1ページ目と一致しない

`1-1` / `1-2` / `2-1` の3件を読み込んで検証した結果:

| | 内容 |
|---|---|
| 画面のプレビュー（「1ページ目」と表示）| 1-1 / 1-2 / **2-1** の3行 |
| 実際のPDFの1ページ目 | 1-1 / 1-2 の2行のみ（2-1 は2ページ目）|

ステータスの「3 questions · 2 pages」は正しいので、**ページ数の見積りだけが分割規則を見ており、プレビュー本体が見ていない**。`previewRows` が `filledRows.slice(0, perPage)` のままセクション分割を無視しているため。`_group_rows` 相当の先頭グループを取る形にすれば一致する。

### ⬜ 8-C. ガイドの記述が実装と食い違う

- 「解答は**オレンジ**などの色で印字します」「標準の**オレンジ**が隠れやすいです」→ 既定色は `#F43F5E`（ローズ）に変更済み
- 「『**PDFを書き出す**』でダウンロードされます」→ 実際のボタン名は「PDFを発行する」

### ⬜ 8-D. 既定色の変更で赤シートの隠蔽性が低下

赤シートは赤成分を通すため、白紙（R=255）との差が小さい色ほど消える。

| プリセット | R値 | 白紙との差 |
|---|---|---|
| オレンジ `#FFA500` | 255 | **0**（最も消える）|
| ローズ `#F43F5E`（現在の既定）| 244 | 11 |
| 赤 `#EF4444` | 239 | 16 |
| 「マゼンタ」`#A855F7` | 168 | **87**（かなり残る）|

画面上の見栄えでローズを既定にしたと思われるが、隠蔽性ではオレンジが有利（実際のシートは透過特性に幅があるため絶対ではない）。あわせて `#A855F7` は色としては紫で、ラベルの「マゼンタ」とも実態が異なり、赤シートではほぼ残る。

### ⬜ 8-E / 8-F. 仕上げ

- **favicon 未指定** — 両ページに `<link rel="icon">` がなく `/favicon.ico` が404になる。`static/blindcard-maker.svg` をここで使うのが自然。
- **モーダルに `role="dialog"` / `aria-modal` がない** — Escape での閉じる操作は実装済みなので、属性の付与とフォーカス管理を足せば揃う。
- **表記の混在** — 「0 questions · 0 pages」だけ英語で、周囲の日本語UIと混ざっている。

## 8-3. 継続中の残件

- ⬜ **README 未作成**（5-3 と同じ）— `/guide` はエンドユーザー向けの説明であり、ビルド手順（`npm run build:css`）・起動手順・フォントライセンスといった開発者向けの情報は依然どこにもない。
- 🔶 **未コミット** — コミットは `b166f87` の1本のみで、作業ツリーに13ファイル分の変更が残っている。

## 8-4. 推奨する着手順

1. **8-A** テストを緑に戻す（ロゴを使うか、テストを現状に合わせるか決める）
2. **8-B** プレビューをセクション分割に対応させる
3. **8-C** ガイドの2か所を実装に合わせる
4. **8-D** 既定色をどうするか判断する（オレンジに戻す／ローズのまま説明を直す）
5. **8-E / 8-F**、README、コミット

